/** Puerto: mandar correos (recuperar contraseña). */
export class EnviadorCorreos {
  /** { para, asunto, texto, html } */
  async enviar(_correo) {
    throw new Error('EnviadorCorreos.enviar() no está implementado');
  }
}
